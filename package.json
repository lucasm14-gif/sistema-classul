{
  "name": "classul",
  "version": "1.0.0",
  "description": "Sistema Classul - Kanban de pedidos com automação WhatsApp (Evolution API)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd web && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5"
  }
}
